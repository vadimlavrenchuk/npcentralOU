# 🚨 ЧТО СЛУЧИЛОСЬ И ЧТО ДЕЛАТЬ

## Проблема
MongoDB Atlas обнаружил, что **пароли базы данных были выложены на GitHub**. Это произошло в документации (`.md` файлах).

## Что найдено
- ❌ Старый пароль `REDACTED_PASSWORD` - в 4 коммитах
- ❌ Новый пароль `REDACTED_PASSWORD` - в документации
- ✅ Файл `backend/.env` НЕ попал в Git (защищен `.gitignore`)

## ✅ Что уже сделано

1. ✅ Пароли удалены из всех документов
2. ✅ Создан скрипт для очистки Git истории
3. ✅ Изменения закоммичены

## 🔴 ЧТО НУЖНО СДЕЛАТЬ ПРЯМО СЕЙЧАС

### 1️⃣ Очистите Git историю (5 минут)

Откройте PowerShell в папке проекта и выполните:

```powershell
.\clean-git-history-fast.ps1
```

Скрипт удалит все пароли из всей истории Git.

### 2️⃣ Загрузите в GitHub (1 минута)

⚠️ **ВНИМАНИЕ:** Это перепишет историю на GitHub!

```powershell
git push origin --force --all
git push origin --force --tags
```

### 3️⃣ Смените пароль MongoDB Atlas (2 минуты)

1. Откройте: https://cloud.mongodb.com/
2. Войдите в проект **MechanicPro**
3. Перейдите: **Security → Database Access**
4. Пользователь: **vadimlavrenchuk** → нажмите **Edit**
5. **Edit Password** → **Autogenerate Secure Password**
6. **СКОПИРУЙТЕ новый пароль** (например: `aB3#xY7$qR9m`)
7. Нажмите **Update User**

### 4️⃣ Обновите локальный backend/.env (1 минута)

Откройте файл:
```
backend\.env
```

Замените строку:
```env
MONGODB_URI=mongodb+srv://vadimlavrenchuk:НОВЫЙ_ПАРОЛЬ_СЮДА@mechanicpro.wjylube.mongodb.net/MechanicPro?retryWrites=true&w=majority&appName=MechanicPro
```

Вместо `НОВЫЙ_ПАРОЛЬ_СЮДА` вставьте скопированный пароль.

### 5️⃣ Проверьте локально (1 минута)

```powershell
cd backend
npm run dev
```

Должны увидеть:
```
✅ MongoDB Atlas connected successfully
🚀 Server is running on port 5000
```

Если ошибка - проверьте правильность пароля в `backend/.env`.

### 6️⃣ Обновите production сервер (5 минут)

```powershell
# Подключитесь
ssh root@kontrollitud.ee

# Перейдите в папку проекта
cd /var/www/mechanic-pro-demo

# Отредактируйте .env
nano .env

# Найдите строку MONGO_URI и замените пароль на новый
# Ctrl+O для сохранения, Enter, Ctrl+X для выхода

# Перезапустите
docker compose restart

# Проверьте логи
docker logs mechanic-pro-demo --tail 30
```

Должны увидеть в логах:
```
✅ MongoDB Atlas connected successfully
```

## ✅ Готово!

После выполнения всех шагов:
- ✅ История Git очищена
- ✅ Старые пароли не работают
- ✅ Новый пароль установлен
- ✅ Backend работает локально и на production

## 📞 Если что-то не работает

**Локально не подключается:**
- Проверьте `backend/.env` - пароль без пробелов
- Перезапустите backend: `npm run dev`

**Production не подключается:**
- Проверьте `/var/www/mechanic-pro-demo/.env` на сервере
- Проверьте логи: `docker logs mechanic-pro-demo`
- Убедитесь что IP сервера добавлен в MongoDB Atlas → Network Access

**Git история не очистилась:**
- Запустите скрипт еще раз: `.\clean-git-history-fast.ps1`
- Проверьте: `git log --all -S "REDACTED_PASSWORD"` (должно быть пусто)

## 📋 Полная документация

Детальные инструкции: [SECURITY_ACTION_PLAN.md](SECURITY_ACTION_PLAN.md)
