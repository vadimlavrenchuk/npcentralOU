# 🚨 ПЛАН ДЕЙСТВИЙ: Устранение утечки учетных данных

## ✅ Выполнено

1. ✅ Проверена Git история - найдены пароли в 4 коммитах
2. ✅ Удалены пароли из документации:
   - `UPDATE_PRODUCTION_PASSWORD.md`
   - `СРОЧНО_СМЕНА_ПАРОЛЯ.md`
   - `EMERGENCY_CHECKLIST.md`
3. ✅ Создан скрипт для очистки Git истории
4. ✅ `.env` файлы в `.gitignore` - защищены

## 🔴 СРОЧНО: Сделайте СЕЙЧАС

### Шаг 1: Очистите Git историю (5 минут)

```powershell
# Запустите скрипт очистки
.\clean-git-history-fast.ps1
```

Этот скрипт:
- Найдет все пароли в истории Git
- Заменит их на `REDACTED_PASSWORD`
- Очистит reflog и сделает сборку мусора

### Шаг 2: Загрузите в GitHub (1 минута)

```powershell
# ВНИМАНИЕ: это перепишет историю на GitHub!
git push origin --force --all
git push origin --force --tags
```

### Шаг 3: Смените пароль MongoDB (3 минуты)

1. Откройте https://cloud.mongodb.com/
2. Войдите в проект **MechanicPro**
3. Перейдите в **Security → Database Access**
4. Найдите пользователя `vadimlavrenchuk`
5. Нажмите **Edit → Edit Password**
6. Выберите **Autogenerate Secure Password**
7. **СКОПИРУЙТЕ** новый пароль
8. Нажмите **Update User**

### Шаг 4: Обновите локальный backend/.env

```powershell
# Откройте файл
code backend\.env

# Замените MONGODB_URI на новый:
# MONGODB_URI=mongodb+srv://vadimlavrenchuk:ВАШ_НОВЫЙ_ПАРОЛЬ@mechanicpro.wjylube.mongodb.net/MechanicPro?retryWrites=true&w=majority&appName=MechanicPro
```

### Шаг 5: Проверьте локально

```powershell
cd backend
npm run dev

# Должны увидеть:
# ✅ MongoDB Atlas connected successfully
```

### Шаг 6: Обновите production сервер

```powershell
# Подключитесь к серверу
ssh root@kontrollitud.ee

# Отредактируйте .env
cd /var/www/mechanic-pro-demo
nano .env

# Обновите строку MONGO_URI с новым паролем
# Сохраните: Ctrl+O, Enter, Ctrl+X

# Перезапустите контейнеры
docker compose restart

# Проверьте логи
docker logs mechanic-pro-demo --tail 30
```

## 📊 Что было скомпрометировано

### Найдено в Git:
- ❌ Старый пароль: `REDACTED_PASSWORD` (4 коммита)
- ❌ Временный пароль: `REDACTED_PASSWORD` (в документации)
- ✅ Файл `ЗАДАЧА_ВЫПОЛНЕНА.md` (удален из истории ранее)

### Файлы с учетными данными:
- `backend/.env` - защищен `.gitignore` ✅
- Документация `.md` - пароли удалены ✅

## 🛡️ Дополнительная защита (опционально)

После смены паролей:

1. **Ограничьте доступ по IP:**
   - MongoDB Atlas → Network Access
   - Добавьте только IP вашего сервера и офиса

2. **Включите аудит:**
   - MongoDB Atlas → Security → Advanced
   - Enable Database Auditing

3. **Установите git-secrets:**
   ```powershell
   # Предотвращает коммит секретов
   choco install git-secrets
   git secrets --install
   git secrets --register-aws
   ```

4. **Регулярная ротация:**
   - Меняйте пароли каждые 90 дней
   - Создайте напоминание в календаре

## ⏱️ Примерное время: 15-20 минут

1. Очистка Git: 5 мин
2. Force push: 1 мин
3. Смена пароля MongoDB: 3 мин
4. Обновление local: 2 мин
5. Обновление production: 5 мин

## 🆘 Помощь

Если что-то не работает:
- Проверьте логи: `docker logs mechanic-pro-demo`
- Проверьте .env: пароль без пробелов и спецсимволов должен быть закодирован
- MongoDB Atlas: убедитесь что IP сервера в whitelist

---

**Статус:** 🔴 КРИТИЧНО - требует немедленных действий
**Приоритет:** P0
**Ответственный:** Vadim
