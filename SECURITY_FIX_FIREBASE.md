# 🔒 СРОЧНОЕ ИСПРАВЛЕНИЕ БЕЗОПАСНОСТИ

## Проблема
API ключ Firebase был обнаружен в публичном GitHub репозитории.

## ✅ Что было сделано (автоматически)

1. **Создан `.env` файл** с текущими секретами
2. **Создан `.env.example`** как шаблон для других разработчиков
3. **Обновлен `firebaseConfig.ts`** - теперь использует переменные окружения
4. **`.gitignore` уже настроен** правильно и игнорирует `.env` файлы

## ⚠️ КРИТИЧЕСКИ ВАЖНО - ЧТО НУЖНО СДЕЛАТЬ СЕЙЧАС

### 1. Ротация скомпрометированного API ключа

**НЕМЕДЛЕННО создайте новый API ключ в Firebase Console:**

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект "mechanicpro-17959"
3. Перейдите в **Project Settings** → **General**
4. В разделе "Your apps" найдите Web App
5. **Сгенерируйте новый API ключ** или удалите старый
6. **Обновите `.env` файл** с новым ключом
7. Удалите или ограничьте старый ключ `AIzaSyCA0J_NCIf4ze7VjU41rEWW3s9Tsu6eQN8`

### 2. Ограничения API ключа

В Firebase Console настройте ограничения для нового ключа:

- **Application restrictions**: HTTP referrers (веб-сайты)
- Добавьте только ваши домены:
  - `localhost:*` (для разработки)
  - `yourdomain.com/*` (для продакшена)
  
- **API restrictions**: Ограничьте только необходимыми API

### 3. Очистка истории Git

**Старый ключ все еще в истории Git!** Используйте один из скриптов:

```powershell
# Быстрый способ (уже есть в проекте)
.\clean-git-history-fast.ps1

# Или полный способ
.\clean-git-history.ps1
```

**После очистки сделайте force push:**
```powershell
git push origin --force --all
git push origin --force --tags
```

⚠️ **ПРЕДУПРЕДИТЕ КОМАНДУ** перед force push!

### 4. Проверка переменных окружения

Убедитесь что приложение работает:

```powershell
# Проверьте что .env файл загружается
npm run dev
```

### 5. Deployment (Production)

Для production сервера добавьте переменные окружения:

**Если используете PM2/Node:**
```bash
# В production сервере создайте .env файл с НОВЫМ ключом
nano .env
# Или через PM2 ecosystem.config.js
```

**Если используете hosting платформу** (Vercel, Netlify, etc):
- Добавьте переменные через их web interface
- НЕ коммитьте .env в репозиторий!

## 📋 Checklist

- [ ] Создан новый API ключ в Firebase Console
- [ ] Обновлен `.env` файл с новым ключом
- [ ] Удален/ограничен старый ключ
- [ ] Настроены ограничения для нового ключа (HTTP referrers + API restrictions)
- [ ] Очищена история Git (используя скрипт)
- [ ] Сделан force push (после предупреждения команды)
- [ ] Проверена работа приложения локально
- [ ] Обновлены переменные окружения на production сервере
- [ ] Команда уведомлена о изменениях

## 🔐 Дополнительные меры безопасности

### Firebase Security Rules
Проверьте правила безопасности:
- Firestore Rules
- Storage Rules  
- Authentication settings

### Monitoring
Настройте мониторинг в Firebase Console:
- Usage & billing
- Alerts для необычной активности

### Secret Scanning
Включите GitHub Secret Scanning:
1. Репозиторий → Settings → Security
2. Включите "Secret scanning"

## Полезные ссылки

- [Firebase API Key Restrictions](https://firebase.google.com/docs/projects/api-keys)
- [Securing Firebase Projects](https://firebase.google.com/docs/projects/learn-more#securing)
- [GitHub Secret Scanning](https://docs.github.com/en/code-security/secret-scanning)

---

**Время исправления:** ${new Date().toISOString()}
**Статус:** ⚠️ Требуется ротация ключа вручную
