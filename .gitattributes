# Normalize text files to LF in repository
* text=auto

# Prevent Git from normalizing files under node_modules (avoid EOL conversion there)
frontend/node_modules/** -text
# Normalize text files by default
* text=auto

# Keep node_modules files binary / do not perform EOL conversion
frontend/node_modules/** -text
