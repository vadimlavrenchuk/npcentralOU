# 🛡️ ЗАЩИТА ОТ УТЕЧКИ ПАРОЛЕЙ - НАСТРОЕНА

## 🔴 ПРОБЛЕМА НАЙДЕНА

**Файл `.env.production` НЕ был в `.gitignore`!**

Вот почему пароли попадали в Git:
- `.gitignore` имел паттерн `.env.*.local` - он не покрывает `.env.production`
- Файл `.env.production` мог быть случайно добавлен в коммит
- Git hooks отсутствовали для автоматической проверки

## ✅ ЧТО СДЕЛАНО

### 1. Обновлен `.gitignore`
Добавлены дополнительные правила:
```
.env
.env.*                    # ← ВАЖНО: покрывает .env.production
.env.local
.env.*.local
*.env
.env.production           # ← явно добавлен
.env.development
.env.staging
```

### 2. Созданы Git Hooks
Автоматическая проверка ПЕРЕД каждым коммитом:

**Файлы:**
- `.git/hooks/pre-commit` (bash)
- `.git/hooks/pre-commit.ps1` (PowerShell)

**Что проверяют:**
- ❌ Блокируют коммит .env файлов
- ❌ Ищут MongoDB connection strings
- ❌ Ищут пароли, API keys, tokens, secrets
- ❌ Проверяют JWT_SECRET
- ✅ Пропускают только безопасные коммиты

### 3. Проверка существующих файлов

Проверьте что `.env.production` удален из Git:
```powershell
git ls-files | Select-String ".env"
```

Если найден - удалите:
```powershell
git rm --cached .env.production
git commit -m "security: Remove .env.production from Git"
```

## 🔒 КАК ТЕПЕРЬ РАБОТАТЬ БЕЗОПАСНО

### Правило #1: Никогда не коммитьте .env файлы
```powershell
# ❌ НЕПРАВИЛЬНО
git add .env
git add .env.production

# ✅ ПРАВИЛЬНО - используйте .env.example
cp .env.example .env
# Редактируйте .env локально
# Git автоматически его игнорирует
```

### Правило #2: Используйте .env.example для шаблонов
```env
# .env.example
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database
JWT_SECRET=your_secret_here
```

### Правило #3: Документируйте в README
```markdown
## Setup

1. Скопируйте `.env.example` в `.env`
2. Замените значения на реальные
3. Никогда не коммитьте `.env`
```

### Правило #4: На production используйте переменные окружения
На сервере установите переменные напрямую:
```bash
export MONGODB_URI="mongodb+srv://..."
export JWT_SECRET="..."
```

Или через Docker:
```yaml
services:
  backend:
    environment:
      - MONGODB_URI=${MONGODB_URI}
```

## 🚨 ЧТО ДЕЛАТЬ ЕСЛИ УЖЕ ЗАКОММИТИЛИ

### 1. Остановитесь немедленно
```powershell
# НЕ делайте git push!
```

### 2. Удалите из последнего коммита
```powershell
# Если еще не запушили
git reset HEAD~1
git add .  # без .env файлов
git commit -m "ваше сообщение"
```

### 3. Если уже запушили - очистите историю
```powershell
# Запустите скрипт очистки
.\clean-git-history-fast.ps1

# Force push
git push origin --force --all
```

### 4. Смените пароли НЕМЕДЛЕННО
- MongoDB Atlas: новый пароль
- JWT_SECRET: новый секрет
- API Keys: перегенерировать

## ✅ ТЕСТИРОВАНИЕ ЗАЩИТЫ

Протестируйте что hooks работают:

```powershell
# 1. Создайте тестовый .env файл
echo "MONGODB_URI=mongodb+srv://user:pass@cluster.net" > test.env

# 2. Попробуйте закоммитить
git add test.env
git commit -m "test"

# Должны увидеть:
# ❌ ОШИБКА: Обнаружен секрет в test.env

# 3. Удалите тестовый файл
git reset HEAD test.env
rm test.env
```

## 📋 ЧЕКЛИСТ БЕЗОПАСНОСТИ

Перед каждым коммитом:
- [ ] Проверили что нет .env файлов: `git status`
- [ ] Проверили измененные файлы: `git diff --cached`
- [ ] Убедились что паролей нет: `git diff --cached | Select-String "mongodb+srv"`
- [ ] Pre-commit hook выполнился успешно
- [ ] Сообщение коммита не содержит паролей

## 🔧 НАСТРОЙКА НА ДРУГИХ МАШИНАХ

Если клонируете репозиторий на другую машину:

```powershell
# Hooks не переносятся автоматически!
# Нужно настроить заново:

cd your-project
git config core.hooksPath .git/hooks

# Или скопируйте хук:
cp .git/hooks/pre-commit.ps1 .git/hooks/pre-commit
```

## 📞 В СЛУЧАЕ ПРОБЛЕМ

1. **Hook не срабатывает?**
   ```powershell
   # Проверьте настройку
   git config core.hooksPath
   
   # Должно быть: .git/hooks
   ```

2. **Нужно временно отключить hook?**
   ```powershell
   # ТОЛЬКО для экстренных случаев!
   git commit --no-verify -m "message"
   ```

3. **Hook блокирует безопасный файл?**
   - Откройте `.git/hooks/pre-commit.ps1`
   - Уточните паттерны проверки
   - Или добавьте исключение

## 🎯 ИТОГ

✅ `.gitignore` обновлен - блокирует `.env.production`  
✅ Git hooks установлены - автоматическая проверка  
✅ Инструкции созданы - знаете как работать безопасно  
✅ Текущие .env файлы защищены  

**Пароли больше не попадут в Git автоматически!** 🛡️

---

**Создано:** 7 февраля 2026  
**Статус:** 🟢 Защита активна
