# ⚡ Шпаргалка для Работы с AI Ассистентом

## 🎯 В Начале Новой Сессии Скажи:

```
Работаем с проектом MechanicPro.
Деплой ТОЛЬКО на verifed-est.ee (НЕ kontrollitud.ee!)
Читай AI_CONTEXT.md и DEPLOYMENT_CONFIG.md
```

## 📝 Структура Документации

1. **AI_CONTEXT.md** - Краткий контекст для AI (2 мин чтения)
2. **DEPLOYMENT_CONFIG.md** - Полная документация деплоя (10 мин)
3. **README.md** - Описание проекта и фич
4. **DEPLOYMENT.md** - Общая информация о деплое

## 🚀 Скрипты Деплоя

```powershell
# Только frontend
.\deploy-frontend.ps1

# Только backend
.\deploy-backend.ps1

# Все сразу (рекомендуется)
.\deploy-all.ps1
```

## 🔑 Ключевая Информация

### Два Домена на Одном Сервере
```
Сервер: kontrollitud.ee (65.109.166.160)
  ├── kontrollitud.ee   → Другой проект (НЕ трогать!)
  └── verifed-est.ee    → MechanicPro (наш проект)
```

### Docker Контейнеры
```bash
mechanic-pro-demo      # Backend (порт 5005)
proxy_app_1            # Nginx с frontend
```

### Nginx Конфиги
```
/data/nginx/proxy_host/
  ├── 1.conf  → kontrollitud.ee (НЕ трогать!)
  └── 2.conf  → verifed-est.ee (наш конфиг)
```

## 💡 Типичные Запросы

### "Нужно обновить frontend"
```powershell
.\deploy-frontend.ps1
```

### "Нужно обновить backend"
```powershell
.\deploy-backend.ps1
```

### "404 ошибка на API"
- Проверь логи: `ssh root@kontrollitud.ee "docker logs mechanic-pro-demo"`
- Возможно нужен редеплой backend: `.\deploy-backend.ps1`

### "Пользователь не может войти"
- Проверь есть ли юзер в БД
- Запусти seed: `ssh root@kontrollitud.ee "docker exec mechanic-pro-demo node dist/config/seed.js"`

## ⚠️ Частые Ошибки

### Ошибка: "Деплоим не на тот домен"
**Если AI предлагает деплоить на kontrollitud.ee:**
```
❌ СТОП! Это неправильный домен!
✅ Нужен verifed-est.ee
```

### Ошибка: "Путаница в путях"
**Frontend пути:**
- На хосте: `/tmp/frontend-verifed/` (временно)
- В контейнере: `/var/www/mechanic-pro-demo/frontend/`

**Backend пути:**
- На хосте: `/var/www/mechanic-pro-demo/backend/`
- В контейнере: `/app/`

### Ошибка: "Контейнер не тот"
**Правильные имена:**
- Backend: `mechanic-pro-demo` (НЕ mechanic-pro-demo-mechanic-app!)
- Nginx: `proxy_app_1`

## 🎓 Примеры Работы с AI

### ✅ Хороший Запрос
```
Нужно добавить новую фичу в backend и задеплоить на verifed-est.ee.
Читай AI_CONTEXT.md
```

### ❌ Плохой Запрос (может привести к путанице)
```
Нужно задеплоить
```
*Проблема: Не указано какой домен, AI может перепутать*

### ✅ Отличный Запрос
```
Проект: MechanicPro (verifed-est.ee, НЕ kontrollitud.ee!)
Задача: Обновить компонент Inventory
См. AI_CONTEXT.md и DEPLOYMENT_CONFIG.md
```

## 📋 Чеклист Перед Деплоем

- [ ] Убедись что работаешь с правильным проектом
- [ ] Проверь что целевой домен - verifed-est.ee
- [ ] Сделай коммит в git
- [ ] Запусти соответствующий скрипт деплоя
- [ ] Проверь логи после деплоя
- [ ] Протестируй на продакшене

## 🔗 Быстрые Ссылки

- **Прод:** https://verifed-est.ee
- **Логин:** admin / admin123
- **SSH:** `ssh root@kontrollitud.ee`
- **Логи Backend:** `ssh root@kontrollitud.ee "docker logs -f mechanic-pro-demo"`
- **Рестарт Backend:** `ssh root@kontrollitud.ee "cd /var/www/mechanic-pro-demo && docker compose restart"`

---

💡 **Совет:** Добавь этот файл в избранное и открывай в начале каждой сессии!
