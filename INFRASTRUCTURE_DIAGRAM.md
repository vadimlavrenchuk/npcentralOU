# 📊 Схема Инфраструктуры MechanicPro

```
┌─────────────────────────────────────────────────────────────────┐
│                    🖥️  Физический Сервер                         │
│                  kontrollitud.ee (65.109.166.160)               │
└─────────────────────────────────────────────────────────────────┘
                                 │
                 ┌───────────────┴───────────────┐
                 │                               │
                 ▼                               ▼
┌────────────────────────────┐  ┌────────────────────────────┐
│   🌐 Домен 1               │  │   🌐 Домен 2               │
│   kontrollitud.ee          │  │   verifed-est.ee           │
│   (ДРУГОЙ ПРОЕКТ!)         │  │   (MECHANIC PRO)           │
│                            │  │                            │
│   ❌ НЕ ТРОГАТЬ!           │  │   ✅ НАШ ПРОЕКТ            │
└────────────────────────────┘  └────────────────────────────┘
                                               │
                        ┌──────────────────────┴──────────────────────┐
                        │                                             │
                        ▼                                             ▼
        ┌───────────────────────────┐              ┌──────────────────────────┐
        │   🐳 Docker: proxy_app_1  │              │  🐳 Docker:              │
        │   (Nginx Proxy Manager)   │              │  mechanic-pro-demo       │
        ├───────────────────────────┤              ├──────────────────────────┤
        │ Конфигурация:             │◄─────────────┤ Backend                  │
        │ /data/nginx/proxy_host/   │   Proxy      │ Port: 5005->5000         │
        │   1.conf → kontrollitud   │   /api/      │                          │
        │   2.conf → verifed-est ✅ │              │ Stack:                   │
        ├───────────────────────────┤              │ - Node.js + Express      │
        │ Frontend:                 │              │ - TypeScript             │
        │ /var/www/mechanic-pro-demo│              │ - JWT Auth               │
        │          /frontend/       │              │                          │
        │   ├── assets/             │              │ Database:                │
        │   ├── index.html          │              │ MongoDB Atlas ☁️         │
        │   └── vite.svg            │              │                          │
        └───────────────────────────┘              └──────────────────────────┘
                    │                                          │
                    ▼                                          ▼
        ┌───────────────────────────┐              ┌──────────────────────────┐
        │   📁 Static Files         │              │  🔌 API Endpoints        │
        │   - HTML/CSS/JS           │              │  /api/auth/login         │
        │   - React SPA             │              │  /api/work-orders        │
        │   - Glassmorphism UI      │              │  /api/equipment          │
        │                           │              │  /api/inventory          │
        └───────────────────────────┘              │  /api/stats              │
                                                   │  /api/reports            │
                                                   └──────────────────────────┘
```

## 🔄 Процесс Деплоя

```
┌──────────────┐
│  Локальный   │
│  Компьютер   │
└──────┬───────┘
       │
       │ npm run build
       ▼
┌──────────────┐
│   dist/      │
│   (bundle)   │
└──────┬───────┘
       │
       │ scp dist/* → /tmp/frontend-verifed/
       ▼
┌──────────────────────────┐
│   Сервер                 │
│   /tmp/frontend-verifed/ │
└──────┬───────────────────┘
       │
       │ docker cp → proxy_app_1
       ▼
┌──────────────────────────┐
│   Nginx Container        │
│   /var/www/mechanic-pro- │
│   demo/frontend/         │
└──────┬───────────────────┘
       │
       │ Nginx serves
       ▼
┌──────────────────────────┐
│   https://verifed-est.ee │
│   (Live Production)      │
└──────────────────────────┘
```

## 🔀 Поток Запросов

```
┌─────────────┐
│   Browser   │
│   (User)    │
└──────┬──────┘
       │
       │ https://verifed-est.ee/api/auth/login
       ▼
┌─────────────────────────┐
│   Nginx (proxy_app_1)   │
│   SSL Termination       │
│   Routing               │
└──────┬──────────────────┘
       │
       │ Proxy to http://172.17.0.1:5005/api/auth/login
       ▼
┌─────────────────────────┐
│   Backend Container     │
│   mechanic-pro-demo     │
│   Express Server        │
└──────┬──────────────────┘
       │
       │ Query Database
       ▼
┌─────────────────────────┐
│   MongoDB Atlas ☁️      │
│   (Cloud Database)      │
└──────┬──────────────────┘
       │
       │ Return Data
       ▼
┌─────────────────────────┐
│   Response → Browser    │
│   { token, user }       │
└─────────────────────────┘
```

## 📂 Структура Файлов на Сервере

```
/var/www/
├── kontrollitud.ee/          ❌ НЕ ТРОГАТЬ!
│   └── frontend/             
│
└── mechanic-pro-demo/        ✅ НАШ ПРОЕКТ
    ├── docker-compose.yml
    ├── Dockerfile
    └── backend/
        ├── src/
        │   ├── server.ts
        │   ├── config/
        │   ├── controllers/
        │   ├── models/
        │   ├── routes/
        │   └── middleware/
        ├── package.json
        └── tsconfig.json

Docker Containers:
├── proxy_app_1
│   └── /var/www/mechanic-pro-demo/frontend/  ← Frontend здесь
│
└── mechanic-pro-demo
    └── /app/  ← Backend код компилируется сюда
```

## 🎯 Команды по Компонентам

### Frontend
```bash
# Локально
npm run build                  # Сборка

# На сервере (через host)
scp dist/* → /tmp/frontend-verifed/

# В контейнере proxy_app_1
/var/www/mechanic-pro-demo/frontend/
```

### Backend
```bash
# Локально
scp backend/src → сервер

# На сервере
cd /var/www/mechanic-pro-demo
docker compose build           # Сборка
docker compose up -d           # Запуск

# В контейнере mechanic-pro-demo
node dist/server.js            # Запуск приложения
```

### База Данных
```bash
# Seed данные
docker exec mechanic-pro-demo node dist/config/seed.js

# Логи
docker logs mechanic-pro-demo
```

## 🔧 Важные Порты

```
Сервер (Host):
├── 80   → HTTP (redirect to HTTPS)
├── 443  → HTTPS (Nginx Proxy Manager)
└── 5005 → Backend (Docker bridge)

Backend Container:
└── 5000 → Express App (mapped to host 5005)

Database:
└── 27017 → MongoDB Atlas (cloud, не на сервере)
```

## 🎨 Цветовая Легенда

- ✅ Зеленый = Наш проект (трогать можно)
- ❌ Красный = Другой проект (НЕ трогать!)
- 🐳 Docker контейнер
- ☁️ Cloud сервис
- 📁 Файловая система
- 🌐 Веб-сервер
