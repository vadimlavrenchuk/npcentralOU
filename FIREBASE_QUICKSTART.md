# 🚀 Quick Start - Firebase Auth Integration

## Быстрый старт (5 минут)

### 1. Установите зависимости (если еще не установлено)
```bash
npm install
```

### 2. Настройте Firebase

#### Вариант A: Быстрый тест (для разработки)
Откройте `src/config/firebaseConfig.ts` и временно замените конфигурацию вашими данными из Firebase Console.

#### Вариант B: Правильный способ (рекомендуется)
1. Скопируйте файл `.env.example` в `.env`:
```bash
copy .env.example .env
```

2. Откройте `.env` и заполните ваши данные Firebase

3. Обновите `src/config/firebaseConfig.ts`:
```typescript
const firebaseConfig = {
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY,
  authDomain: import.meta.env.VITE_FIREBASE_AUTH_DOMAIN,
  projectId: import.meta.env.VITE_FIREBASE_PROJECT_ID,
  storageBucket: import.meta.env.VITE_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: import.meta.env.VITE_FIREBASE_MESSAGING_SENDER_ID,
  appId: import.meta.env.VITE_FIREBASE_APP_ID
};
```

### 3. Запустите приложение
```bash
npm run dev
```

### 4. Тестируйте!
- Откройте браузер → вас перенаправит на страницу логина
- Зарегистрируйте нового пользователя или войдите через Google
- После входа вы увидите Dashboard
- Ваше имя и аватар отобразятся в header

## 📖 Подробная документация

Смотрите [FIREBASE_SETUP.md](./FIREBASE_SETUP.md) для полной инструкции по настройке Firebase.

## ✅ Что работает:

- ✅ Регистрация новых пользователей (Email + Password)
- ✅ Вход через Email/Password
- ✅ Вход через Google
- ✅ Защищенные маршруты (требуют авторизации)
- ✅ Отображение профиля в header
- ✅ Выход из системы
- ✅ Автоматический редирект на login при отсутствии авторизации

## 🎨 Дизайн

Страница входа выполнена в стиле **Glassmorphism** с:
- Анимированным фоном
- Прозрачным размытым стеклом
- Плавными переходами
- Адаптивным дизайном

## 🔐 Безопасность

⚠️ **Не забудьте**: 
- Файл `.env` уже добавлен в `.gitignore`
- Никогда не коммитьте реальные Firebase ключи в публичный репозиторий
- Используйте переменные окружения для production

## 🆘 Помощь

Если что-то не работает:
1. Проверьте, что Firebase проект создан
2. Убедитесь, что Authentication включен в Firebase Console
3. Проверьте правильность данных в `firebaseConfig.ts` или `.env`
4. Перезапустите dev сервер после изменения `.env`

Подробнее в [FIREBASE_SETUP.md](./FIREBASE_SETUP.md)
